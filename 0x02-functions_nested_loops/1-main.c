#include "main.h"

/**
 * main - Entry point of the program
 *
 * This program calls the print_alphabet function to print all the lowercase alphabets to
 * the standard output.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Call the print_alphabet function to print all the lowercase alphabets */
	print_alphabet();

	/* Return 0 to indicate that the program executed successfully */
	return (0);
}
